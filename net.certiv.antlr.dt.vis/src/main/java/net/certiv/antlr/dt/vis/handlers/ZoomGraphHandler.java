package net.certiv.antlr.dt.vis.handlers;

import org.eclipse.core.commands.AbstractHandler;
import org.eclipse.core.commands.ExecutionEvent;
import org.eclipse.core.commands.ExecutionException;

public class ZoomGraphHandler extends AbstractHandler {

	public ZoomGraphHandler() {
		super();
	}

	@Override
	public Object execute(ExecutionEvent event) throws ExecutionException {
		// CallGraphEditor editor = CallGraphPlugin.getDefault().getEditor();
		// CallGraphViewer viewer = (CallGraphViewer) editor.getZoomableViewer();
		// String action = event.getParameter(CallGraphEditor.MENU_ZOOM);
		// viewer.setZoomAsText(action);
		return null;
	}

}
